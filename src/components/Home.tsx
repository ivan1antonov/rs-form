const Home = () => {
  // const worker = new Worker('../assets/owid-co2-data.json', import.meta.url);

  return (
    <table className="table">
      <tr>
        <th>Country</th>
        <th>Population</th>
        <th>ISO code</th>
      </tr>
      <tr></tr>
    </table>
  );
};

export default Home;
